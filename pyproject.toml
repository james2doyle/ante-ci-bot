[project]
name = "ante-ci-bot"
version = "1.0.0"
description = "Composite GitHub Action that runs the ante headless CLI to review PRs"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
